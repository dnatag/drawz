[package]
name = "drawz-core"
version = "0.1.0"
edition = "2021"
description = "Semantic diagram renderer for agent-to-human communication in terminals"

[dependencies]
serde = { version = "1", features = ["derive"] }
unicode-width = "0.2"
